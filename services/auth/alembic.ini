[alembic]
script_location = migrations
# DATABASE_URL env is read in migrations/env.py; this fallback is for `alembic` CLI outside docker
sqlalchemy.url = postgresql+psycopg://flowix:flowix@localhost:5432/flowix

[loggers]
keys = root

[handlers]
keys =

[formatters]
keys =
